1)
SET largest_number to zero
PROMPT user to enter numbers greater than zero
    IF user enters zero
        STOP loop
IF number > largest_number
THEN 
   SET largest_number TO number
THEN
PRINT largest_number

2) 
PROMPT user input first name
THEN
    SET first_name TO user input
THEN
PRINT text "Hello World", + first_name

3) 
SET sum + count to 0
THEN
    PROMPT user to enter a number or enter "calculate" to end
THEN
    IF input is "calculate"
    END loop
THEN
    ADD inptut to total
THEN
    SET arithmetic_mean which = sum / count
THEN
    PINT "The arithmetic mean is", arithmetic_mean

4) 
PROMPT grocery list from user
THEN
    END loop when user marks value "complete"
THEN
    SORT list of products missing from "complete" marking in alphabetical order
PRINT missing product list in alphabetical order

5) 
PROMPT user to enter value X 
PROMPT user to enter value Y
THEN
    SET value S to X + Y
    SET value P to X * Y
    SET value A to S / 2
THEN
        PRINT S, A and P separately

